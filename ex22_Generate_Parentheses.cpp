#include <iostream>
using namespace std;

class Solution:
public:
    void solve(string op, int open, int close, vector<string> &ans) {
        if(open == 0 && close == 0) {
            ans.push_back(op);
            return;
        }
        o
    }